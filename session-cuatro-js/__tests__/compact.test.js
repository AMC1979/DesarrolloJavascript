
import { suma , compact } from "../compact";


/*describe('compact function',()=>{
    test('Remove falsy', ()=>{
        const input = [0,1,false,2,'',3]
        const output = [1,2,3]

        expect(compact(input)).toEqual(output)
    })
    
    test('Error', ()=>{
        expect(()=>{
            compact([])
        }).toThrow()
    })
}   )*/

/*describe('Compact function' , ()=>{
      test('Remove falsy', ()=>{
        const input = [0,1,false,2,'',3]
        const output = [1,2,3]

        expect(compact(input)).toEqual(output)
    })

    //test mas 
    test('sumatoria',()=> { 

        const input = [2, 3, 5]
        const output = 10
   
        expect(suma(input)).toEqual(output)
   
       })*/


    /*test ('Array cannot be empty',()=>{
    expect(()=>{ compact([]) }).toThrow()
    })*/

    

/*test('it removes falsy values',()=>{

        //imput 
        const input = [];

        //output
        const output = [1,2,3]; //lo que se espera

        //expect(compact(input)).toEqual(output)
        expect(compact(input).toThrow())

    })*/
 
//})